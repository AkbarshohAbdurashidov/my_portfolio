import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Portfolio smoke test', (WidgetTester tester) async {
    // Portfolio app - UI is tested manually in browser
    expect(true, isTrue);
  });
}